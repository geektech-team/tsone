name: Deploy Docs to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'packages/tsone/**'
      - 'packages/one/**'
      - 'packages/tsone-cli/**'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build documentation
        # The docs site is served from a GitHub Pages project sub-path
        # (e.g. https://<owner>.github.io/tsone/), so the base path is the
        # repository name.
        run: bun run docs:build -- --base=/${{ github.event.repository.name }}/

      - name: Build One UI documentation
        # One docs are published under the same Pages site, merged into
        # packages/tsone/docs/dist/one/ (e.g.
        # https://<owner>.github.io/tsone/one/). The out-dir must be absolute
        # because the script resolves it against the package working dir.
        run: bun run docs:one:build -- --base=/${{ github.event.repository.name }}/one/ --out-dir=${{ github.workspace }}/packages/tsone/docs/dist/one

      - name: Build TSone CLI documentation
        # CLI docs are published under the same Pages site, merged into
        # packages/tsone/docs/dist/cli/ (e.g.
        # https://<owner>.github.io/tsone/cli/).
        run: bun run docs:cli:build -- --base=/${{ github.event.repository.name }}/cli/ --out-dir=${{ github.workspace }}/packages/tsone/docs/dist/cli

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: packages/tsone/docs/dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
