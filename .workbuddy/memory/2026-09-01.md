# 2026-09-01 工作日志

## one 库扩展 5 组组件（@geektech/one）

为 `packages/one` 组件库新增 5 组组件，完整走通「实现 → 导出 → 分类注册 → 单测 → 文档/示例 → 验证」流程：

- **Radio / RadioGroup**（表单类）：`lib/radio/OneRadio.ts` + `OneRadioGroup.ts`，原生 radio 语义，复用 `OneSelectOption`，受控/非受控。
- **Avatar**（数据展示类）：`lib/avatar/OneAvatar.ts`，`src` 图片 / `text`/children 回退，`circle`/`square` 形状。
- **Progress**（数据展示类）：`lib/progress/OneProgress.ts`，`role=progressbar`，百分比钳制 0–100，内联 `width` 驱动。
- **Loading**（反馈类）：`lib/loading/OneLoading.ts`，`role=status`，纯 CSS 静态圆环（StyleManager 不支持 @keyframes）。
- **TimePicker**（表单类）：`lib/time-picker/OneTimePicker.ts`，封装原生 `input type=time`，`step` 非法回退 60。

接线：`lib/index.ts` 导出、`categories.ts` 分类注册、`style-contract`/`component-types`/`package-smoke`/`categories` 契约测试同步。

文档：新增 5 个 demo 组件（`docs/app/demos/`）、`demo-examples.ts` 注册、`DocArticle.ts` 静态预览、`styles.ts` 样式引入、`client.ts` 注册、content 页（form/data-display/feedback）重排 order 并新增 radio/time-picker/avatar/progress/loading 页、README 双语更新、docs 测试同步（APPROVED_PATHS 25→30、docs-build 页数 25→30）。

验证：`bunx tsc --noEmit` 通过；`bun test packages/one` 217 pass / 2 fail（均为既有版本不一致）；`bun run build` 通过；`bun pm pack --dry-run` 通过（91 文件，含 6 个新组件 d.ts）。

## OneInput 归类 + TimePicker 重构

- **OneInput 移到表单分类**：`categories.ts` 的 `basic` 改为仅 `['OneButton']`，`OneInput` 移到 `form`（`OneFormItem` 之后）。同步 `categories.test.ts`、README 双语分类清单、`docs/app/content/form.ts` 的 signature。
- **OneTimePicker 重构**：从原生 `input type=time` 改为「可编辑文本输入（`type=text`，光标可编辑）+ 点击展开时/分/秒下拉面板」。面板按 `min`/`max` 限制小时范围，`step`（秒）决定分钟步进与是否显示秒列（`step < 60`）。点击「时」更新值但面板保持打开，点「分/秒」更新并关闭；Escape 关闭；disabled/readonly 不展开。保留受控/非受控/表单注入语义。
  - 样式重构：`.one-time-picker`（容器）+ `.one-time-picker__input` + `.one-time-picker__panel` + `__column` + `__option--selected`；`style-contract` 的 border 断言从 `one-time-picker-base` 改为 `one-time-picker-input`。
  - 重写 `time-picker.test.ts`（10 用例：文本输入、展开/关闭、分/时选择、秒列、min/max、disabled/readonly、受控恢复、文本事件）。

验证：`bunx tsc --noEmit` 通过；`bun test packages/one` 223 pass / 2 fail（仍为既有版本不一致）；`bun run build` 通过；`bun pm pack --dry-run` 通过。

## 遗留问题（非本次引入，未处理）

`packages/one` 版本不一致：`package.json` `version: "0.1.0"` + peer `>=0.1.0 <0.2.0`，但 `lib/index.ts` `ONE_VERSION = '0.0.1'`，且 `package-contract.test.ts`（期望 0.0.1 / `>=0.0.2 <0.1.0`）与 `package-smoke.test.ts`（按 0.0.1 找 tarball）未同步。需决定目标版本（倾向 0.1.0）并同步三处。
